{
  "version": 2,
  "buildCommand": "prisma generate",
  "builds": [
    {
      "src": "./server/Server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/Server.js"
    }
  ]
}
